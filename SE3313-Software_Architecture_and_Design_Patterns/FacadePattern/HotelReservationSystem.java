public class HotelReservationSystem {
    public void reserve(String destinationCity, String travelTime, String DepartureDate) {
        System.out.println("Hotel Reserved in " + destinationCity  + " for " + travelTime + " days " + "Starting from " +DepartureDate);
    }
}
