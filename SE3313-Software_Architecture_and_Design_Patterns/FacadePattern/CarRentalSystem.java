public class CarRentalSystem {
    public void reserve(String destinationCity, String travelTime, String departureDate) {
        System.out.println("Car Rented in " + destinationCity  + " for " + travelTime + " days " + "Starting from " +departureDate);
    }
}
